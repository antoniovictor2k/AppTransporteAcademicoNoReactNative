import { StyleSheet } from 'react-native';

const styleTelaCarregando = StyleSheet.create({

  container: {
    flex: 1,
    justifyContent: 'center',
    textAlign: 'center',
    justifyItems: 'center',
    alignItems: 'center',
    backgroundColor: '#113036',
  },

  texto: {
    color: '#fff',
  }

})

export default styleTelaCarregando;